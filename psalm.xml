<?xml version="1.0"?>
<psalm
    errorLevel="1"
    useDocblockPropertyTypes="true"
    usePhpDocMethodsWithoutMagicCall="true"
    strictBinaryOperands="true"
    findUnusedBaselineEntry="true"
    findUnusedCode="false"
    hideExternalErrors="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
>
    <stubs>
        <file name="vendor/inpsyde/wp-stubs-versions/latest.php" />
    </stubs>

    <projectFiles>
        <directory name="src"/>
        <ignoreFiles>
            <directory name="vendor" />
        </ignoreFiles>
    </projectFiles>

   <issueHandlers>
        <MixedAssignment errorLevel="suppress" />
        <RedundantCastGivenDocblockType errorLevel="suppress" />
        <MissingClassConstType errorLevel="suppress" />
        <!-- Psalm 6-only rules. Static analysis CI runs on PHP 8.1+ (Psalm 6). -->
        <!-- Keep the class non-final for BC; factory methods are already final. -->
        <ClassMustBeFinal errorLevel="suppress" />
        <!-- Package still supports PHP 7.4, so #[\Override] cannot be used yet. -->
        <MissingOverrideAttribute errorLevel="suppress" />
    </issueHandlers>
</psalm>
